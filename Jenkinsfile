pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building project'
                sh 'ls -lah'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests'
            }
        }
    }
}
